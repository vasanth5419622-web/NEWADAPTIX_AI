version: '3.8'

services:
  adaptix-farm:
    build: .
    container_name: adaptix_farm_app
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=production
      - PORT=8000
      - HOST=0.0.0.0
    volumes:
      - ./data:/app/data
    restart: unless-stopped
